"""
Python Demo DEV

SELECT `id`,`name`,`manufacture` FROM obj_server_models

thunder lock: disabled (you can enable it with --thunder-lock)

spawned uWSGI master process 

提交测试

线上修改：
   合并冲突测试---------


线下修改：
    合并冲突测试-----------------

=============================================================

new a tag v1

git commit --amend


git reset --hard 460cd86 in 202106021918
merge:
git commit 202105141500 m_demo

update desk

 assert group_code in [0, 1, 2], '坐标轴（{}，{}）座位状态有误'.format(x_coord, y_coord)
            assert status in [1, 2, 3], '坐标轴（{}，{}）座位属性有误'.format(x_coord, y_coord)
This is a change
update in 202106021918
"""

